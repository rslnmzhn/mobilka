# mobilka roadmap

## Urgent — Android storage blocker
- [ ] 30. Автоматизированное исправление SAF готово и проверено: сохранённый нормализованный root-document URI теперь сопоставляется с persisted tree grant по строгой canonical identity (scheme + authority + decoded tree document ID), при этом исходный operational URI и namespace recovery journal не изменяются; child/different authority/tree, malformed URI, read-only/write-only/revoked grants покрыты регрессионными тестами. Осталась обязательная проверка на реальном Android-устройстве после перезапуска и повторного выбора той же папки: `list_skills`, `read_skill`, `write_skill`, `read_session_notes`, `write_session_notes` и создание session artifact. До этой проверки пункт не считается завершённым. Не добавлять `MANAGE_EXTERNAL_STORAGE`; при provider-specific сбое отдельно предложить совместимый provider/app-private storage или явно одобренное архитектурное решение.
- [x] 31. Добавлена отдельная локализованная кнопка копирования полного исходного `message.content` для каждого непустого сообщения пользователя и ассистента: без reasoning, metadata, статусов, tool cards, вложений и соседних сообщений; только явная запись clipboard, Material tooltip/semantics/keyboard access, RU/EN success/failure feedback и проверки на 320 px.
- [ ] 32. После отдельного явного сигнала владельца полностью переписать `README.md` как красивую проектную витрину mobilka, а не сплошной технический текст: ясный hero-блок и ценностное предложение, ключевые возможности, архитектура и безопасность простым языком, поддерживаемые платформы, установка/первый запуск, галерея интерфейса, roadmap/status и ссылки для разработчиков. Сохранить фирменный стиль mobilka без декоративных градиентов и чужого брендинга; подготовить структуру к RU/EN без дублирования устаревающих фактов. До сигнала README не изменять. Для обновления запросить у владельца актуальные скриншоты без API-ключей, личных данных и чувствительного содержимого: мобильный чат в dark и light theme; desktop adaptive shell; выбор модели; экран Agents/персон; управление Markdown memory; tool-call cards; session artifacts/предпросмотр документа; настройки endpoint с замазанными секретами; updater — только если его UX готов. Желательно PNG/WebP одного масштаба, без системных уведомлений и лишней обрезки; перед публикацией согласовать финальный набор и подписи.

## Milestone M1 — Memory core (user/soul/memory)
- [x] 1. Новые имена и стартовое содержимое файлов памяти: `user.md`, `soul.md`, `memory.md`.
- [x] 2. Идемпотентная миграция legacy-имён с `.migrated.bak` копиями; `project_context.md` выведен из схемы.
- [ ] 3. Ручная проверка миграции на реальной папке памяти (Android + desktop).
- [x] 4. Мягкий лимит размера memory.md при instant-записи (защита от переполнения).

## Milestone M2 — Personas
- [x] 5. `PersonaRegistry`: парсинг personas.yaml, персистентная активная персона.
- [x] 6. Инструменты `switch_persona` / `list_personas` / `save_persona` / `delete_persona`: переключение по естественной просьбе; создание/правка/удаление персон через confirm-flow diff.
- [ ] 7. Ручная проверка: смена персоны в чате, создание новой, выключение.

## Milestone M3 — Prompt hardening
- [x] 8. `PromptGuard`: скан на prompt injection (inline-маркер), вырезание YAML-frontmatter. Лимитов длины нет.

## Milestone M4 — Agent & workspace
- [x] 9. Skills: агент сам пишет/читает/листает свои навыки в `<storage>/skills/<name>.md` (`write_skill` / `read_skill` / `list_skills`); Android-SAF fallback. Владелец подтвердил на реальном Android-устройстве успешные list/read/write skills.
- [ ] 10. Сессии: папки `sessions/<yyyy-MM-DD_название>/` c `session.md` (контекст для смены модели) и `artifacts/` для артефактов; инструменты `write_session_notes` / `read_session_notes`. Android-SAF fallback.
- [x] 11. Артефакты пишутся в папку сессии автоматически (docx рядом с .md источником).
- [x] 12. Документация для ИИ (`docs/ai/`): карта архитектуры, инвентарь инструментов, соглашения проекта — чтобы агент не читал весь репозиторий.
- [x] 13. Реализован и автоматизированно проверен `read_public_source`: direct HTTPS transport с DNS/IP pinning, ручными redirects, cancellation, безопасными MIME/charset, лимитом 1 МиБ на вызов и UTF-8 chunks ≤256 КиБ. Persisted Conversation хранит fail-closed совокупный wire budget 8 МиБ; cache miss консервативно резервирует allowance до открытия transport, unused bytes возвращаются при штатном завершении, crash оставляет reservation списанным, cache hit бесплатен, некорректный persisted counter считается исчерпанным. Canonical URL ограничен 8 КиБ, alias metadata — 8/resource и 64/conversation. Единый RequestToolSecurityState помечает только текущий request как untrusted; mutating/sensitive/unknown и instant memory.md требуют persisted exact-call confirmation. Решение терминально завершает request; orphan executing восстанавливается как indeterminate без повторного исполнения. PromptGuard остаётся эвристикой. Focused, analyze и полный suite (556 тестов) пройдены без live internet.
- [x] 14. Новый системный промпт дефолтного агента под полную схему.

## Milestone M5 — Shell polish
- [x] 15. Нижняя панель и десктопный рельс: только иконки, тултипы на десктопе.
- [x] 16. Регресс-тесты шела на 320px без подписей.
- [x] 19. Внеплановая коррекция shell UI реализована и автоматизированно проверена: точный `/chat` на узком экране имеет нулевой постоянный footprint без dock/handle/inset, а навигация открывается модальным overlay только центральным направленным вверх жестом в пустом чате или у newest-bottom, localized semantics action либо Ctrl+Shift+N. Остальные узкие маршруты, включая `/chat/artifacts`, сохраняют 52px NavigationBar; desktop breakpoints неизменны. Исправлены адаптивный empty state, локализованное отображение автоматического/резервного заголовка и пустого model ID. Физическая проверка жестов остаётся ручной частью пункта 18.
- [x] 20. Автоматизированно реализованы и проверены канонические внутренние `mobilka-artifact:` ссылки с явным MD/DOCX, свежей проверкой metadata/неизменяемого владельца, fail-closed no-follow разрешением только app-private файлов и единым безопасным native opener для чата, сессии и каталога. DOCX имеет persisted SHA-256 исходного Markdown: edit атомарно инвалидирует файл/hash с rollback, export при сбое восстанавливает точные прежние bytes/hash либо переводит representation в fail-closed, legacy/устаревший DOCX требует re-export. Catalog/store пропускают corrupt records, UI показывает только фиксированные локализованные ошибки, `generate_docx` возвращает URI без путей даже при сбое mirror. Focused, analyze и полный suite (641 тест) пройдены. Физическая Android/desktop проверка native viewer остаётся ручной частью пункта 18.

## Milestone M6 — Discovery & mobile tools
- [ ] 21. Перенести персоны из монолитного `personas.yaml` в отдельные человекочитаемые файлы `personas/<slug>.md`: frontmatter хранит ID, название, краткое описание и параметры, тело — системный промпт. `list_personas` возвращает только компактный каталог метаданных, а содержимое загружается по имени только для выбранной персоны. Добавить идемпотентную миграцию с резервной копией, проверкой коллизий, безопасными confirm-flow мутациями и восстановлением; не создавать второй изменяемый индекс как конкурирующий источник истины.
- [ ] 22. Добавить интернет-поиск как отдельный инструмент `web_search`, дополняющий, но не дублирующий HTTPS-чтение из пункта 13: поиск возвращает компактные заголовки, URL и snippets, после чего агент явно выбирает страницы для безопасного чтения. До реализации выбрать один из вариантов провайдера:
  - BYOK Search API через общий адаптер (например, Brave Search, Tavily, Serper или совместимый сервис) — предсказуемый JSON, но внешний ключ, стоимость и политика приватности;
  - собственный SearXNG — больше контроля и несколько источников, но пользователь обслуживает сервер;
  - нативный web-search выбранного AI-провайдера — простая интеграция, но vendor lock-in и неодинаковое поведение моделей;
  - прямой разбор HTML поисковиков — только как нежелательный fallback из-за нестабильности и условий использования.
  Независимо от варианта: отключаемая функция, явный индикатор сетевого запроса, локаль/период/число результатов, лимиты и тайм-ауты, отсутствие секретов, данных диалога и устройства в запросах и логах, документированная privacy/retention policy провайдера, дедупликация URL и обработка запросов/snippets/страниц как недоверенного ввода через `PromptGuard`. Для SSRF-защиты применять canonical URL parsing, разрешать только HTTP(S), проверять все IPv4/IPv6-адреса DNS-ответа, отклонять loopback/private/link-local/reserved/metadata destinations, защищаться от DNS rebinding с привязкой соединения к проверенному адресу и повторять полную проверку каждого redirect.
- [ ] 23. Android-терминал, Termux, Shizuku, root и произвольную shell не реализовывать. Модель получает только workspace, созданный и неизменно привязанный к текущей сессии, и работает внутри него через типизированные инструменты из пункта 24. Доступ за пределы session workspace, shell parser, запуск произвольных процессов и privilege escalation не предоставлять. Вернуться к идее терминала можно только как к отдельному будущему архитектурному решению после нового явного одобрения владельца.
- [ ] 24. Добавить типизированные SAF-first инструменты для workspace, созданного текущей сессией: `list_files`, `search_files`, `read_file`, `write_file`, `apply_patch`, `move_file`, `delete_file`, `make_directory`. Инструменты не принимают произвольный корневой путь и всегда используют immutable workspace binding исходного запроса; мутации выполняются только внутри `sessions/<session>/`, без `MANAGE_EXTERNAL_STORAGE`, с точным preview/diff и подтверждением каждой мутации. Непосредственно перед записью повторно проверять binding, SAF scope и target, лимиты и квоты, защищаться от traversal/symlink escape и TOCTOU, использовать atomic write/recovery journal и безопасно отказываться от операции, если SAF-провайдер не позволяет подтвердить результат.
- [ ] 25. Добавить инструменты извлечения содержимого документов: PDF, DOCX, таблицы и изображения/OCR; обрабатывать локально, когда возможно, требовать отдельное явное подтверждение отправки каждого документа удалённой модели, ограничивать размер/число страниц/распаковку/ресурсы и сохранять provenance до исходного файла. Парсеры не должны исполнять макросы и скрипты, разрешать внешние сущности/ресурсы или выполнять скрытые сетевые запросы.
- [ ] 26. Отложено без реализации и не входит в текущий scope: управляемые Android intents для карт, календаря, напоминаний, share-sheet, dialer и email. Вернуться к проектированию только по отдельному запросу владельца, когда будет понятен ожидаемый UX.
- [ ] 27. Возможная будущая функция, не проектировать и не реализовывать без предварительного полного явного одобрения владельца: фоновые и периодические задачи агента, WorkManager/foreground service и уведомления о результате. Даже после одобрения потребуется отдельный security/UX design; текущие инструменты работают только в активной пользовательской сессии и не получают фоновых полномочий.
- [ ] 28. Добавить безопасный HTTP/API-клиент для пользовательских интеграций: presets с exact canonical-host allowlist, разрешёнными методами и схемой авторизации; секреты только в secure storage; preview запроса с редактированием чувствительных заголовков; лимиты тела/ответа; управляемая политика proxy. Проверять все адреса DNS-ответа, защищаться от DNS rebinding, отклонять локальные/private/link-local/reserved/metadata destinations, полностью проверять каждый redirect, ограничивать смену origin и безусловно удалять authorization при любой смене origin. Произвольный unrestricted HTTP выключен по умолчанию.
- [ ] 29. Реализовать простой и понятный UX прикрепления документов и фотографий к сообщению: одна заметная кнопка вложений, системные file/photo pickers, multi-select, preview до отправки, удаление отдельных вложений, отображение имени/типа/размера и понятные ошибки лимитов. Выбранные файлы копировать во временное app-private staging с provenance, привязывать к immutable request/conversation IDs, очищать по завершении жизненного цикла и явно показывать, какие вложения будут отправлены удалённой модели. Поддержать Android SAF и desktop `file_selector`, безопасные MIME/размер/количество, downscale/compress изображений против OOM и обработку документов через пункт 25. Буфер обмена не читать; не запрашивать broad storage или постоянный доступ ко всей медиатеке.

## Milestone M7 — Validation
- [ ] 17. Полный analyze/test после всех пунктов; обновление fixture-тестов памяти.
- [ ] 18. Ручная проверка владельцем: миграция старых файлов, персоны, skills, session notes, docx-экспорт на устройстве.
- [ ] 33. Request-scoped Android foreground service использует `stopWithTask: false`, lease остаётся единственным normal stop owner; ошибка запуска не прерывает запрос, а privacy-safe outcomes диагностируются. Автоматизированный fix проверен; физическая проверка pause/hidden/background и notification permission обязательна, force-stop survival не заявляется.
- [x] 34. В Settings добавлены локализованные статус и прямой выбор/reselect workspace-папки только через memory controller, без broad storage permissions. Автоматизировано; физический reselect остаётся в пункте 30.
- [x] 35. После первого финального успешного ответа добавлено одно изолированное non-streaming автозаглавие с persistent claim, bound model, строгим parser/timeout/fallback и защитой manual rename; единая conversation-scoped очередь сериализует authoritative mutations и streaming snapshots, а title completion изменяет только title-поля последнего состояния. Focused tests и analyze пройдены; ручная проверка с реальным endpoint остаётся отдельной.
- [x] 36. Действия сообщений перенесены в локализованную доступную поверхность по long press/secondary click/keyboard: точное Copy и identity-bound Send again для user-сообщений. Заголовок показывает активный чат и независимо открываемую модель; отдельные history/model/artifact icons удалены, New chat сохранён. Touch-only arena gesture распознаёт поздний широкий горизонтальный свайп из безопасной области чата, отклоняет вертикальное/обратное/долгое и multipointer-намерение; keyboard/semantics alternatives сохранены. Автоматизировано; UX жестов требует физической проверки.
- [ ] 37. Глобальный каталог артефактов реализован как вложенный экран Chat: immutable ownership/provenance, current-session filtering, поиск, ownership/file-availability filters и стабильные сортировки работают только по app-private metadata/files без новых копий и сканирования workspace mirrors. Представления MD/DOCX обновляются после create/edit/export/share/delete из фактических app-private файлов; stat fail-closed проверяет containment, regular-file и no-follow identity. Legacy/unowned отделены от удалённых/недоступных владельцев с session provenance, а session sheet без conversation закрыт для глобальных данных и создания. Focused tests, analyze и полный suite (472 теста) пройдены; debug APK ранее заблокирован недоступным Android Gradle Plugin 8.6.0. Ручная проверка жестов, открытия/экспорта и адаптивного UX на физическом устройстве остаётся обязательной.
- [ ] 38. Добавить ограниченную очистку staged-файлов автообновления, чтобы APK/MSI и незавершённые `.part` не накапливались: при повторном запуске приложения после обновления удалять только файлы из канонического app-owned каталога `updates`, которые относятся к уже установленной или устаревшей версии, сохраняя текущий проверенный пакет, пока установка либо повторная попытка ещё возможна. Перед новой загрузкой удалять устаревшие и лишние пакеты по version/age/count policy; не следовать symlink/reparse point и не удалять ничего вне `updates`. На Android выполнять post-update cleanup после сравнения установленного versionCode с метаданными staged APK, учитывая отсутствие надёжного результата системного installer. На Windows сохранять MSI до завершения detached handoff, а после следующего успешного запуска новой версии удалять его и ротировать `%TEMP%\mobilka-update-handoff.log`; при сбое оставлять один проверенный пакет для retry. Добавить persisted staged-update metadata, startup recovery, bounded cleanup и тесты success/failure/cancel/restart без удаления актуального установщика.
- [x] 39. Исправлены три регрессии чата и публичных источников: production `read_public_source` теперь выполняет TLS после прямого TCP к каждому заранее проверенному адресу с исходным hostname для SNI/проверки сертификата, последовательным IPv6/IPv4 fallback, общим deadline/cancellation и privacy-safe диагностикой; TLS handshake владеет реальным `RawSocket`, который закрывается при cancel/deadline, а поздние connect-task/socket/secure результаты немедленно отменяются и закрываются без TLS/fallback после победившей отмены. Ошибки безопасно проецируются в tool card и не объясняются моделью домыслами. Восстановленный/переключённый чат использует обратный список с логическим bottom=0, показывает новейшие сообщения и следует за потоком только пока пользователь закреплён внизу; touch, wheel/trackpad и прочие user-scroll уведомления отделены от программного `jumpTo`. Заголовок модели остался единым локализованным control, получил accent-цвет и встроенную tune-иконку без переполнения на 320 px. Dedicated widget/transport tests покрывают async и initial restore, switch/follow/touch/wheel browsing/resume/order/320 px, TCP/TLS cancellation/deadline abort, поздние task/socket/error результаты, повторный cancel/late completion, единый deadline и отсутствие HTTP до TLS; `flutter analyze --no-pub` и полный suite (580 тестов) пройдены без live internet.
- [x] 40. Request-scoped post-success skill reflection подтверждён: held final answer неизменен, доступны только list/read/propose, bounded rounds/timeout/cancellation и ровно один inspectable persisted proposal; model-authored skill никогда не записывается до explicit confirmation.
- [x] 41. Финальный security review: confirmation-free model skill creation удалён, каждый model-authored create/update сначала сохраняется как exact proposal и требует explicit confirmation. Ledger имеет одного persisted Conversation owner: outcomes атомарно append-ятся через authoritative mutation, concurrent append сериализован, provenance читается из exact persisted ledger, а reflection fail-closed сравнивает полное ordered content и sticky state, не только count. Ledger fail-closed декодирует malformed/forward trust, bounded проверяет persisted entries, сохраняет newest 32 и sticky discarded-untrusted state; discarded uncertain/untrusted provenance остаётся source-derived, запрещает automatic eligibility и отображается в proposal warning. `read_skill` возвращает JSON data envelope без forgeable XML delimiters. Markdown links проходят strict canonicalization и external-application launcher с safe async feedback. Proposal E2E покрывает exact create/update, no-write-before-confirm, stale hash, reject, concurrent claim, quota, changed authority и executing recovery. Changed-feature complexity blockers устранены; analyze и полный suite (614 тестов) пройдены.

## Future architecture — Chat / Advanced Coding
- [ ] Добавить отдельный режим Advanced Coding рядом с текущим Chat, не меняя текущее поведение чата до отдельного проектирования.
- [ ] Спроектировать отдельный каталог `.md` coding-агентов; не объединять его автоматически с текущим каталогом агентов.
- [ ] Разрешать ИИ создавать или изменять coding-агентов только по явному запросу пользователя и после явного подтверждения точного изменения.
- [ ] Определить отдельные политики инструментов, моделей и workspace для Coding mode.
- [ ] Решить будущее размещение текущего каталога агентов относительно Chat и Advanced Coding mode.
