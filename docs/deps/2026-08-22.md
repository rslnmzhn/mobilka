# Dependency audit — 2026-08-22

result: clean

## Summary
package_managers: Dart pub / Flutter pub
total_vulnerabilities: critical:0 high:0 medium:0 low:0
outdated_major: 0
outdated_minor: 0
unlocked: 0

Scope: final re-audit of the current `pubspec.yaml` and `pubspec.lock`. Counts describe confirmed findings in the changed dependency graph, not an assertion that pre-existing dependency maintenance debt has disappeared.

## Vulnerabilities

No confirmed critical, high, medium, or low vulnerability was found in the dependency change. No dependency blocker remains.

The current Dart SDK has no dedicated `pub audit` command. Both `flutter pub outdated --json` and `flutter pub get --dry-run --enforce-lockfile` were attempted, but configured pub.dev access returned HTTP 403 while fetching advisory/version metadata. This limits a fresh whole-graph online scan but does not indicate a dependency defect; no result was fabricated.

## Outdated (major version gaps only)

None identified in the changed dependency graph. `cryptography` resolves as version 2.9.0 from its immutable Git commit.

## Unlocked versions

None. `cryptography` is pinned to the full 40-character commit SHA `87f42fad6f7f120feb8e47854164016ceac3dfd0`; manifest `ref`, lockfile `ref`, and lockfile `resolved-ref` all match. GitHub confirms this immutable commit exists, is signature-verified, and contains `cryptography` version 2.9.0.

## Lock-diff verification

- `package_info_plus` and `package_info_plus_platform_interface` are absent from both the manifest and lockfile.
- Relative to Git, the only `pubspec.lock` change is the expected addition of `cryptography` 2.9.0 at the pinned Git commit.
- Relative to Git, the only `pubspec.yaml` dependency change is that same `cryptography` addition. The separate version change is intentional.
- No `flutter_secure_storage` or `flutter_secure_storage_windows` lock change remains; the Windows implementation remains 4.2.2.
- No `win32` lock change remains; it remains 6.4.0.
- No `go_router` lock change remains; it remains 17.4.0.
- The prior incidental `ffi_leak_tracker` removal and `vm_service` update are absent; they remain 0.1.2 and 15.2.0 respectively.
- `git diff --check -- pubspec.yaml pubspec.lock` reports no whitespace errors.
- There are no staged changes to either dependency file.

## Version decision

The manifest change from `1.0.0+1` to `0.0.1+1` remains. Version `0.0.1` is explicitly intentional and is therefore not a blocker or unintended package change.

## Remaining blockers

None.
