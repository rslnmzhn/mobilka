# Dependency audit — 2026-08-09

## Summary
package_managers: Dart pub / Flutter pub
total_vulnerabilities: critical:0 high:0 medium:0 low:0
outdated_major: 4
outdated_minor: 1
unlocked: 0

Audit inputs: `pubspec.yaml` and `pubspec.lock`; Flutter 3.38.5 / Dart 3.10.4. This audit specifically rechecks the graph after adding `flutter_markdown_plus` 1.0.12.

No dependency blocker was found for the Markdown-rendering milestone. `flutter_markdown_plus` is current, resolves without conflict, and is correctly represented in the lockfile.

## Milestone blockers

**None from `flutter_markdown_plus` or lock consistency.**

The two discontinued build-system transitives described below are existing development-tooling debt. They do not sit on the `flutter_markdown_plus` runtime path and do not block this milestone under the current pinned Flutter/Dart toolchain.

## `flutter_markdown_plus` verification

- Manifest constraint: `^1.0.12`; locked version: `1.0.12`; latest pub.dev version: `1.0.12`.
- The lockfile checksum `fce641d6c2106cc495de1cd603f97a4f9d615a97a64011928ded380dbdade935` matches the pub.dev archive checksum.
- Package requirements are Dart `^3.4.0` and Flutter `>=3.27.1`; the project uses Dart 3.10.4 and Flutter 3.38.5.
- Its resolved runtime dependencies are `markdown` 7.3.1, `meta` 1.17.0, and `path` 1.9.1. All satisfy the package constraints (`^7.3.1`, `^1.16.0`, and `^1.9.1`).
- Pub reports no advisory, retraction, or discontinuation for the locked package. The pub.dev advisory endpoints also report no advisory for `flutter_markdown_plus` or its Markdown parser dependency, `markdown`.
- The package was published 2026-07-10, has a verified publisher, and receives 160/160 pub.dev score points; it is not dormant or deprecated.
- The package has only three hosted runtime dependencies and does not introduce a large transitive tree.

## Vulnerabilities

No locked package is marked `isCurrentAffectedByAdvisory` by `dart pub outdated --json`. Pub.dev's package advisory API reports no advisory for `flutter_markdown_plus` or `markdown`.

A dedicated OSV scan was not available because `osv-scanner` is not installed and this Dart SDK has no `pub audit` command. Consequently, zero means “none reported by available pub.dev advisory metadata,” not a guarantee that the graph is vulnerability-free.

## Discontinued and retracted packages

| package | version | status | path | impact |
|---------|---------|--------|------|--------|
| build_resolvers | 2.4.2 | discontinued | `build_runner` (dev only) | Existing generator-stack maintenance risk; unrelated to Markdown runtime |
| build_runner_core | 7.3.2 | discontinued | `build_runner` (dev only) | Existing generator-stack maintenance risk; unrelated to Markdown runtime |

No locked version is retracted. No direct dependency is marked discontinued.

## Outdated (major version gaps only)
| package | current | latest | breaking_changes |
|---------|---------|--------|-----------------|
| flutter_riverpod | 2.5.1 | 3.4.2 | Coordinate Riverpod runtime, annotations, generator, generated code, and tests |
| flutter_secure_storage | 10.3.1 | 11.0.0 | Review platform configuration and API-key persistence migration |
| riverpod_annotation | 2.3.5 | 4.0.6 | Coordinate with `flutter_riverpod` and `riverpod_generator` |
| riverpod_generator (dev) | 2.4.0 | 4.0.8 | Coordinate generator/analyzer migration and regenerate outputs |

`build_runner` 2.4.13 → 2.16.0 is also more than two minor versions behind. These gaps predate and are independent of `flutter_markdown_plus`.

## Unlocked versions

None. No direct dependency uses `*`, `any`, or `latest`. Caret constraints are bounded semantic-version ranges and all hosted packages are concretely locked with checksums.

## Compatibility, conflicts, and lock consistency

- `flutter pub get --dry-run --enforce-lockfile` succeeded and reported **“No dependencies would change.”**
- The lockfile declares Dart `>=3.10.4 <4.0.0` and Flutter `>=3.38.4`; the installed Dart 3.10.4 / Flutter 3.38.5 satisfy both.
- No solver conflict or duplicate logical package version was found. Dart pub selected one version per package.
- `flutter_markdown_plus` adds the already-compatible `markdown` 7.3.1 path and does not perturb the Riverpod/analyzer generator stack.
- Pub reports 33 packages with newer versions incompatible with current constraints. This is existing modernization debt, not evidence of an inconsistent lockfile.
- Existing exact pins on the Riverpod/build-generator set intentionally preserve compatibility for Flutter 3.38 / Dart 3.10; upgrading one package in isolation remains unsafe.

## Risky patterns

- No production dependency is an obvious dev-only tool; `build_runner`, `riverpod_generator`, test SDK, and lints remain under `dev_dependencies`.
- No new dependency with 50+ transitives was introduced. `flutter_markdown_plus` has three hosted runtime dependencies.
- No deprecation/discontinuation or stale-publication concern applies to `flutter_markdown_plus`.
- Existing non-blocking maintenance risks remain: the discontinued build transitives above and the old stable Hive/Hive Flutter packages.

## Recommendation

Proceed with the Markdown-rendering milestone. Keep the existing Riverpod/build-generator modernization and dedicated OSV scanning as separate maintenance work; neither requires changing this milestone's dependency addition.

No manifest or lockfile was modified.
